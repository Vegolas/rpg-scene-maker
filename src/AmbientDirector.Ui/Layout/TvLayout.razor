@inherits LayoutComponentBase

@* Chrome-less layout for the player-facing /tv display: no nav rail, no top bar, no tab bar — the page
   owns the whole viewport. Everything else keeps the default MainLayout (see App.razor). *@
@Body
